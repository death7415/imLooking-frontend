2026-08-03 23:54:53 +05:30
- Completed the next three remaining Phase 1 auth milestones in order by replacing the `/consent` placeholder with a dedicated consent checkpoint, centralizing reusable auth validation rules, and wiring the frontend auth routes into a working placeholder flow.
- Added `src/features/auth/model/auth-validation.js`, expanded `auth-session.js`, and updated the auth model exports so phone, email, password, confirm-password, and required-field validation plus temporary local auth-session persistence can be reused across auth screens.
- Added `ConsentScreen` and its styling, routed age-gate into consent, routed consent back into signup, routed signup and reset-password back into login with success state, and routed login into either the originally requested protected route or `/home`.
- Updated `LoginScreen`, `SignupScreen`, `ForgotPasswordScreen`, `ResetPasswordScreen`, `AgeGateScreen`, `ConsentPage.jsx`, `phases.md`, `memory.md`, and `arch.md` so the documented auth architecture and milestone state match the new behavior.
- Verified Milestones 9, 11, and 12 with a successful production `pnpm build`.

2026-08-03 21:55:55 +05:30
- Completed the next three Phase 1 milestones in order by replacing the `forgot-password`, `reset-password`, and `age-gate` placeholder route panels with dedicated auth screens.
- Added `ForgotPasswordScreen`, `ResetPasswordScreen`, and `AgeGateScreen` plus route-specific styling, all built on the shared auth feature structure and reusable auth components instead of one-off markup.
- Wired forgot-password to controlled identifier recovery messaging, reset-password to password-entry plus invalid-or-expired token states, and age-gate to explicit eligible-versus-underage branching with a continue path into consent.
- Updated `src/features/auth/index.js`, `src/features/auth/screens/index.js`, the three route pages, and the architecture docs so the stricter auth feature API and milestone status match the implemented screens.
- Verified Milestones 6, 7, and 8 with a successful production `pnpm build`.

2026-08-03 21:48:38 +05:30
- Tightened the auth feature into a stricter industry-style package boundary by splitting it into `components`, `screens`, and `model`, then adding `src/features/auth/index.js` as the public feature API consumed by pages.
- Moved route-level auth surfaces into `screens`, kept reusable primitives in `components`, added a `model/index.js` entry point, and updated page imports so route pages no longer reach into auth internals directly.
- Simplified screen-level imports to consume the auth `components` barrel, removed the empty legacy `src/features/auth/ui` shell, and updated `arch.md`, `memory.md`, and `phases.md` so the documented structure matches the repo.
- During verification, corrected the moved login and signup screen imports for `route-paths.js`, then re-verified the stricter auth feature refactor with a successful production `pnpm build`.

2026-08-03 20:58:08 +05:30
- Refactored the auth UI layer from one mixed folder into granular intent-based packages: `brand`, `experiences`, `feedback`, `fields`, `layout`, and `navigation`.
- Moved the existing auth JSX and CSS files into those smaller packages, added package-level barrel files, and updated auth pages plus internal component imports so the structure is easier to scan without hunting across one oversized directory.
- Updated `arch.md`, `memory.md`, and `phases.md` to record the new auth package-structure rule and keep the documented architecture aligned with the repo.
- During verification, corrected the moved login and signup experience imports for `route-paths.js`, then re-verified the full refactor with a successful production `pnpm build`.

2026-08-03 20:45:20 +05:30
- Corrected a wording typo in the immediately prior `change.log` entry so the alternate login-path note reads clearly.

2026-08-03 20:44:53 +05:30
- Removed the redundant phone-number chooser from the default login view so the screen now opens directly on the phone field instead of showing the selected phone path twice.
- Kept the alternate auth path reusable by reusing `AuthMethodSwitch` as a single-option alternate-method prompt, which now offers email-or-username plus password from the phone-first login flow and phone-number OTP from the identity flow.
- Updated `LoginExperience.jsx`, `LoginExperience.css`, `phases.md`, `memory.md`, and `arch.md` to reflect the phone-first login contract and re-verified the change with a successful production `pnpm build`.

2026-08-03 20:36:15 +05:30
- Refactored the login experience into two reusable entry paths: phone number with OTP-oriented entry and email-or-username with password-based entry.
- Added a reusable `AuthMethodSwitch` component and styling so the login method choice can be reused across auth surfaces instead of being hardcoded inside one screen.
- Updated `LoginExperience.jsx` and `LoginExperience.css` so the phone path validates a mobile number and shows OTP placeholder status, while the email-or-username path keeps password entry plus forgot-password navigation.
- Updated `phases.md`, `memory.md`, and `arch.md` to record the refined login contract and the new reusable auth method-switch primitive.
- Verified the login refactor with a successful production `pnpm build`.

2026-08-03 20:32:37 +05:30
- Recorded a previously unlogged repo addition before starting new work, as required by the project workflow.
- Found an untracked local asset at `images/brand-logo.png` and logged its presence so the repo history stays in sync before the next auth change.

2026-08-03 18:42:39 +05:30
- Smoothed the empty-state signup DOB-and-age row by aligning the split-field grid to the top and reserving consistent helper-message height beneath both columns.
- Updated `SignupExperience.css` so the fresh form no longer shows a visually uneven age field area when the DOB helper wraps and the age helper does not.
- Re-verified the styling adjustment with a successful production `pnpm build`.

2026-08-03 18:38:57 +05:30
- Corrected the signup age derivation so it now uses the current date for a proper years-and-months calculation instead of showing only whole years.
- Changed the signup age field to display values such as `1 yr 10 mons` or `2 yrs 3 mons`, and aligned the 18+ gate with that same age-part calculation.
- Added a future-date guard for DOB and re-verified the change with a successful production `pnpm build`.

2026-08-03 18:28:47 +05:30
- Tightened the signup validation follow-up by adding explicit required-field messages for blank email, mobile number, and password states instead of falling through to verification-only copy.
- Removed the now-unused `AuthInlineError` import from `SignupExperience.jsx` after switching the final signup status message to helper-text presentation.
- Re-verified the signup flow changes with another successful production `pnpm build`.

2026-08-03 18:27:48 +05:30
- Refined the Phase 1 signup implementation so it now captures full name, email, mobile number, username, password, confirm password, date of birth, and auto-derived age instead of the earlier smaller placeholder field set.
- Added frontend email-format validation, password-confirmation handling, 18+ age restriction handling, and a debounced username-availability check that waits for typing to settle before evaluating a temporary local availability list.
- Extended the shared auth text-field primitive and form-field styling to support inline field actions, then used that to add `Send OTP` actions for both email and mobile with temporary frontend demo auto-verification states.
- Updated `phases.md`, `memory.md`, and `arch.md` so Milestone 5, signup behavior, and the temporary OTP and username-check placeholder decisions match the current implementation.
- Verified the refactor with a successful production `pnpm build`.

2026-08-03 18:22:24 +05:30
- Recorded a previously unlogged manual repo edit before starting new work, as required by the project workflow.
- Updated `rules.md` with an explicit instruction to always add `TODO` markers for steps that still need attention, API calls, or backend work.

2026-08-03 16:19:43 +05:30
- Continued in order and completed Phase 1 Milestone 5 by replacing the signup placeholder route with a dedicated frontend signup experience.
- Added `SignupExperience.jsx` and `SignupExperience.css` with controlled name, identifier, and password fields, consent checkboxes for legal/community acknowledgments, a create-account CTA, disabled/loading behavior, and a generic error state.
- Updated `SignupPage.jsx` to render the new signup experience and documented the milestone completion in `phases.md`, `memory.md`, and `arch.md`.
- Kept real backend signup requests deferred to the later auth-flow wiring milestone while making the signup screen production-shaped at the frontend layer.

2026-08-03 16:19:42 +05:30
- Fixed the forgot-password route UX gap by adding a clear `Back to login` action.
- Extended the shared `AuthRoutePanel` with an optional footer slot so route-specific actions can be added without breaking the common auth placeholder layout.
- Wired the forgot-password page to use that new footer slot with a secondary `Back to login` button that navigates to `/login`.
- Updated `memory.md` to record the shared auth-route-panel footer-action pattern as a durable auth-layout decision.

2026-08-03 16:19:41 +05:30
- Completed Phase 1 Milestone 4 in order by turning the login screen from a static placeholder form into a stateful frontend experience.
- Added controlled identifier and password input state to `LoginExperience`, plus a real submit handler, disabled CTA behavior, loading feedback, and a visible generic error state.
- Updated shared auth behavior so the password reveal toggle respects disabled form state, and updated the shared button styling to show a proper disabled state.
- Updated `phases.md`, `memory.md`, and `arch.md` to record Milestone 4 as complete while keeping real backend auth requests deferred to the later auth-flow wiring milestone.

2026-08-03 13:47:06 +05:30
- Fixed the legal-page return-flow issue by making auth legal links carry their origin route in navigation state.
- Added a shared `PublicPolicyPage` wrapper so Terms, Privacy, and Community Guidelines now show a back button and can return users to the route they came from.
- Extended `FoundationPanel` with an optional footer slot so the public policy surfaces can expose a consistent back action without duplicating layout markup.
- Added a fallback policy-page return behavior: use explicit origin when present, otherwise browser history when available, otherwise return to `/login`.

2026-08-03 13:47:05 +05:30
- Returned to the agreed sequence and completed Phase 1 Milestone 3 instead of skipping ahead again.
- Added shared auth form primitives in `src/features/auth/ui`: `AuthTextField`, `AuthPasswordField`, `AuthCheckboxField`, `AuthHelperText`, `AuthInlineError`, `AuthSubmitButton`, plus shared field styling.
- Refactored the current login surface to consume the new shared auth text, password, helper-text, and submit primitives instead of keeping field and button markup styled ad hoc inside `LoginExperience`.
- Updated `phases.md`, `arch.md`, and `memory.md` to record Milestone 3 as complete and document the new shared auth primitive layer for the remaining Phase 1 screens.

2026-08-03 13:47:04 +05:30
- Progressed Phase 1 by implementing the new legal-links milestone instead of leaving it only as planning text.
- Added a reusable `AuthLegalLinks` component and shared styling so auth surfaces can expose Terms, Privacy Policy, and Community Guidelines from one place.
- Wired the shared legal-links footer into the shared `AuthRoutePanel`, which covers signup plus the current forgot-password, reset-password, age-gate, and consent placeholder routes.
- Wired the same legal-links pattern into the current login surface and replaced raw auth `<a href>` links there with router `Link` usage for internal navigation.
- Updated `phases.md`, `memory.md`, and `arch.md` to record Milestone 10 as complete and document the shared auth legal-links pattern.

2026-08-03 13:47:03 +05:30
- Added a dedicated Phase 1 milestone for placing Terms, Privacy, and community-guidelines links on signup and other required auth or trust-gate surfaces, and renumbered the later Phase 1 milestones accordingly in `phases.md`.
- Added a single frontend env setup with `.env` and `.env.example`, setting `VITE_API_BASE_URL` to `https://imlooking.onrender.com`.
- Updated `.gitignore` to ignore local env files while keeping `.env.example` trackable.
- Added centralized shared API configuration in `src/shared/config/env.js` and `src/shared/config/api.js` so backend base URL handling and endpoint paths are not hardcoded inside feature components.
- Updated `arch.md` and `memory.md` to record the new env and endpoint-registry decisions as durable project architecture.

2026-08-02 23:59:59 +05:30
- Completed Impeccable `init` for the repo by adding a root `PRODUCT.md` product-truth file aligned to the installed skill's schema and current project evidence.
- Captured the current confirmed product posture for `imLooking`: web-first, trust-first, privacy-first, serious-relationship dating UX with app-store-safe and accessibility constraints.
- Updated `memory.md` so future repo work treats `PRODUCT.md` as the durable Impeccable product-context source alongside the existing planning docs.
- Re-checked repo status and proceeded without finding a new unlogged manual repo edit that needed to be logged first.
- Proved the installed Impeccable setup on the existing login surface by running the bundled detector against `src/features/auth/ui/LoginExperience.jsx` and validating the project with a successful production `pnpm build`.

2026-08-02 23:59:58 +05:30
- Installed the three requested UI/UX toolsets at the project level following their docs: Impeccable via `npx impeccable install`, Taste Skill via `npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"`, and Emil Kowalski's bundle via `npx skills add emilkowalski/skill`.
- Confirmed the installs landed under `.agents/skills`, including `impeccable`, `design-taste-frontend`, `emil-design-eng`, `animation-vocabulary`, `apple-design`, `find-animation-opportunities`, `improve-animations`, `pick-ui-library`, `prototype`, and `review-animations`.
- Recorded the installed skill-tooling stack in `memory.md` as a durable project setup decision for future UI/UX work.
- The installs also added `skills-lock.json`, and Impeccable reported its project hook installation under `.agents`.

2026-08-02 23:59:41 +05:30
- Completed Phase 1 Milestone 2 by introducing reusable auth layout primitives instead of keeping the auth stage structure split across one-off route implementations.
- Added shared `AuthStageShell`, `AuthStageCard`, `AuthBrandDock`, and `AuthRoutePanel` components so splash, login, and placeholder auth routes now sit on one responsive auth foundation.
- Refactored `LoginExperience` and `LoadingExperience` to consume the shared auth shell/card system and moved the remaining auth placeholders off the generic `FoundationPanel` onto the dedicated auth-route panel.
- Updated `arch.md`, `memory.md`, and `phases.md` to record Milestone 2 as complete and document the reusable auth layout-shell decision.
- Verified the Milestone 2 auth-layout refactor with successful lint, test, build, and performance-budget checks.

2026-08-02 23:59:23 +05:30
- Completed the remaining Phase 0 foundation gaps so the phase can now be closed honestly instead of only relabeled.
- Added public policy surfaces for Terms, Privacy, and community guidelines, plus a route-level error fallback page.
- Added shared accessibility primitives for buttons, inputs, dialogs, sheets, toasts, and navigation so the baseline component layer now exists before deeper feature work.
- Added a measurable Phase 0 quality gate with `lint`, `test`, `build`, and `perf:check`, including a shared performance-budget config and baseline validation scripts.
- Updated `arch.md`, `memory.md`, and `phases.md` to mark Phase 0 as closed and document the new foundation baseline.
- Verified the Phase 0 closure baseline with successful lint, test, build, and performance-budget checks.

2026-08-02 23:36:10 +05:30
- Added a temporary auth-session reader and a dedicated protected-route wrapper so unauthenticated users cannot reach `/home`, `/chat`, or `/onboarding` by manually changing the URL.
- Updated the router so app-shell routes now pass through `ProtectedAppRoute` and redirect guests back to `/login` with the attempted path preserved in route state.
- Updated `arch.md`, `memory.md`, and `phases.md` to record the new protected-route placeholder behavior in Phase 1.
- Verified the protected-route placeholder with a successful `pnpm build`.

2026-08-02 23:32:07 +05:30
- Reworked the auth splash and login surfaces again after the reference-image review so they now follow a denser milky liquid-glass slab pattern instead of a lighter generic glass card.
- Kept the existing romantic color family and dark backdrop, but changed the auth panels to use a darker center core, brighter rim lighting, softer internal refraction, and cloudy white buildup near the lower edge.
- Tightened the form layout to a more centered embedded-pill composition so the inputs and CTA read more like controls inside the same glass object.
- Updated `design.md` and `memory.md` to capture the darker-core plus milky-lower-cloud liquid-glass direction.
- Verified the auth liquid-glass redesign with a successful `pnpm build`.

2026-08-02 23:26:53 +05:30
- Corrected the previous liquid-glass pass after review because it changed the overall backdrop too much and still did not make the surfaces feel different enough.
- Restored the darker romantic/adventure background direction and moved the liquid-glass emphasis back into shared shell, panel, splash, login, input, button, and nav surfaces.
- Strengthened the reusable blob geometry and reflective/refraction styling so the fluid look is visible in actual components instead of reading like only a palette change.
- Updated `design.md` and `memory.md` to reflect the corrected direction: dark atmospheric backdrop with liquid behavior expressed primarily by the UI surfaces.
- Verified the corrected liquid-glass direction with a successful `pnpm build`.

2026-08-02 23:19:47 +05:30
- Shifted the shared theme from darker romantic glass toward a softer pearl liquid-glass direction with pale frosted gradients and water-like translucency.
- Added global blob-shape tokens for shell, panel, card, hero, pill, input, and ambient-orb surfaces so the fluid form language is reusable across the project.
- Reworked shared shell, foundation panel, auth boundary, loading splash, login card, inputs, button, and brand styling to use lighter watery highlights, softer reflections, and less rigid geometry.
- Updated `design.md` and `memory.md` to document the new pearl/blob liquid-glass direction as the global design baseline.
- Verified the global liquid-glass theme shift with a successful `pnpm build`.

2026-08-02 23:06:57 +05:30
- Pushed the new theme further toward a stronger liquid-glass treatment after visual review.
- Increased refraction-style highlights, edge sheen, inner glass glow, and translucent layering across shared shell, panel, splash, and login surfaces.
- Updated shared liquid-glass tokens so the effect reads less like standard frosted glass and more like a fluid, reflective surface.
- Updated `design.md` and `memory.md` to reflect the stronger caustic/highlight-driven liquid-glass direction.
- Verified the stronger liquid-glass refinement with a successful `pnpm build`.

2026-08-02 23:02:44 +05:30
- Shifted the frontend theme toward a more romantic and adventure-leaning palette while keeping the product background and surfaces gradient-led rather than solid.
- Updated shared design tokens and global background styling to use warmer dusk tones, sunset accents, cool escape highlights, and stronger layered gradients.
- Reworked shell, panel, splash, auth-wrapper, and login surfaces to feel more like liquid glass with brighter highlights, richer translucency, and stronger blur/saturation treatment.
- Updated `design.md` and `memory.md` to reflect the new romantic liquid-glass visual direction replacing the older glassmorphism wording.
- Verified the theme shift with a successful `pnpm build`.

2026-08-02 22:01:13 +05:30
- Reverted the heavier last eye pass after review and replaced it with a simpler eyelash treatment.
- Removed the glossy, overworked 3D additions and switched to thinner, separate upper lashes so the eyes read more like human eyelashes.
- Kept the eye form dimensional but toned the effect back to subtle depth instead of strong rounded gloss.
- Updated `design.md` and `memory.md` to reflect the finer upper-lash treatment and softer eye depth wording.
- Verified the eyelash correction with a successful `pnpm build`.

2026-08-02 21:32:27 +05:30
- Refined the shared `imLooking` eye styling again so the eyes now include visible lash detail instead of reading like flat colored lids.
- Added stronger 3D depth with rounded sclera shading, richer iris layering, gloss highlights, and deeper lid shadow treatment.
- Reworked the top-lid edge so it reads more like lashes or fine hair rather than a simple color band.
- Updated `design.md` and `memory.md` to reflect the lash detail and stronger 3D eye treatment in the animated brand mark.
- Verified the lash-and-depth refinement with a successful `pnpm build`.

2026-08-02 21:28:42 +05:30
- Refined the shared `imLooking` eye styling so the eyes now read with clearer, more realistic eyelids.
- Increased the open-state lid presence so the upper and lower lids remain visible even when the eyes are not blinking.
- Added more human eyelid detail with crease, lash-line, waterline, and softer eye-corner treatment in `AuthBrandMark.css`.
- Updated `design.md` and `memory.md` to reflect the stronger eyelid treatment in the animated brand mark.
- Verified the eyelid refinement with a successful `pnpm build`.

2026-08-02 21:22:21 +05:30
- Added a true first-load auth splash so the app now opens on an animated loading page before moving into the login form.
- Added reusable `imLooking` brand animation primitives in `AuthBrandMark.jsx` and `AuthBrandMark.css` so the same animated mark can be used across the splash and login experiences.
- Added `LoadingExperience.jsx`, `LoadingExperience.css`, and `pages/loading/LoadingPage.jsx` to render the branded loading screen and auto-transition to `/login`.
- Updated the router so `/` is now the animated loading entry route instead of an immediate redirect to `/login`.
- Simplified `LoginExperience.jsx` to consume the shared brand component while keeping the login form as the second step in the auth flow.
- Updated `design.md`, `memory.md`, `arch.md`, and `phases.md` to reflect the new splash-first auth entry behavior.
- Verified the new splash-first loading flow with a successful `pnpm build`.

2026-08-02 21:03:39 +05:30
- Corrected the login-brand positioning after visual review using the shared screenshot feedback.
- Moved the `imlooking` lockup back above the login card so it drops from above onto the modal edge instead of living inside the modal body.
- Changed the brand animation so `iml`, the eyes, and `king` each fall in as separate parts rather than one grouped block.
- Removed the stray dock connector box that was appearing under the eyes and reading like a random extra surface.
- Updated `design.md` and `memory.md` to reflect the above-modal brand landing behavior and cleaned-up dock treatment.
- Verified the corrected login landing with a successful `pnpm build`.

2026-08-01 02:48:29 +05:30
- Refined the login landing again so the brand treatment now feels attached to the login modal instead of reading like a second floating card.
- Moved the `imlooking` dock inside the login modal structure and removed the extra glass-panel treatment around the brand copy area.
- Tightened the `imlooking` lockup layout so it stays on one line more reliably while the eyes remain inline as the `oo`.
- Reworked the eye styling again with a more human shape, stronger lids, softer iris tones, and a cleaner docked presentation.
- Updated `design.md` and `memory.md` to reflect the integrated auth-brand treatment.
- Verified the revised login landing with a successful `pnpm build`.

2026-08-01 02:44:51 +05:30
- Refined the animated login landing after visual review feedback.
- Moved the `imlooking` brand treatment so it lands on and docks to the login modal instead of sitting in a separate panel.
- Kept the `imlooking` lockup on a single line to prevent `king` from wrapping below the eyes.
- Reworked the eyes to feel more human with stronger eye shape, visible upper and lower lids, iris detail, and more natural look/blink motion.
- Updated `design.md` and `memory.md` to reflect the corrected auth-brand treatment.
- Verified the revised login animation with a successful `pnpm build`.

2026-08-01 02:36:38 +05:30
- Started Phase 1 Milestone 2 and the visual part of Milestone 4 by replacing the login placeholder with an animated auth landing.
- Added `src/features/auth/ui/LoginExperience.jsx` and `LoginExperience.css` for the dedicated login-stage UI.
- Animated the `imlooking` brand lockup so the word drops from above, the eyes land last on the brand mark, and the pupils blink and look left/right continuously.
- Removed the leftover auth-boundary planning header so the login experience owns the full first impression.
- Updated `arch.md`, `phases.md`, `design.md`, and `memory.md` to reflect the new auth-stage implementation.
- Verified the animated login landing with a successful `pnpm build`.

2026-08-01 02:27:53 +05:30
- Removed the `Back to Home` link from the auth boundary so users cannot visually bypass the login-first entry flow.
- Updated `src/widgets/auth-route-boundary/AuthRouteBoundary.jsx` to keep the auth routes focused on authentication only.
- Verified the auth-boundary change with a successful `pnpm build`.

2026-08-01 02:25:21 +05:30
- Changed the default landing behavior so the app now opens on the login page.
- Updated the router so `/` redirects to `/login` instead of `/home`.
- Updated `arch.md` and `memory.md` to reflect the new root-route behavior.
- Verified the landing-route change with a successful `pnpm build`.

2026-08-01 02:21:04 +05:30
- Completed Phase 1 Milestone 1: auth route planning.
- Added route constants and codified the auth route map in the router.
- Added dedicated auth-boundary routing for `/login`, `/signup`, `/forgot-password`, `/reset-password`, `/age-gate`, and `/consent`.
- Kept `/home`, `/onboarding`, and `/chat` inside the shared app shell.
- Added TODO-marked placeholder pages for the new auth and safety-gate routes.
- Updated `arch.md`, `phases.md`, and `memory.md` to reflect the route split and milestone progress.
- Verified the milestone with a successful `pnpm build`.

2026-08-01 02:17:34 +05:30
- Detected and recorded a previously unlogged manual repo change in `rules.md`.
- Added rule: always add `TODO` markers for steps that still need attention, API calls, or backend work.
- This entry was added after re-checking repo diffs against `change.log`.

2026-08-01 02:14:09 +05:30
- Updated `phases.md` to replace the broad Phase 1 bullets with the agreed small authentication milestones and recommended build order.

2026-08-01 02:07:43 +05:30
- Clarified the mandatory `change.log` workflow for all future repo work.
- New operating rule: before touching the repo, first read `change.log`.
- New operating rule: after reading `change.log`, inspect current repo diffs/status and detect any unlogged manual changes.
- New operating rule: if user-made or previously unlogged changes are found, add them to `change.log` before starting new requested work.
- New operating rule: after completing the requested work, append the new change entry at the top of `change.log`.
- Updated `rules.md` and `memory.md` so this process is permanent and not reminder-based.

2026-08-01 01:47:47 +05:30
- Completed the first foundation sprint for the frontend project.
- Added `react-router-dom` and established route-based navigation for `/home`, `/login`, `/onboarding`, and `/chat`.
- Replaced the single-screen starter with a modular structure using `app`, `pages`, `widgets`, and `shared` layers.
- Added root providers, a reusable app shell, shared foundation panel UI, and token/global style files.
- Verified the new foundation with a successful `pnpm build`.
- Updated `arch.md`, `phases.md`, `design.md`, and `memory.md` to reflect the implemented structure and current Phase 0 progress.
- Normalized timestamp format wording in docs to use ASCII `+/-HH:MM`.

2026-08-01 01:41:53 +05:30
- Corrected the project logging rule: `change.log` must record every change, including punctuation-only, spacing-only, and tiny text edits.
- Updated `rules.md` and `memory.md` to remove the weaker "meaningful change" wording.

2026-08-01 01:40:23 +05:30
- Updated `change.log` policy to require full local timestamps on every entry.
- Format rule: `YYYY-MM-DD HH:mm:ss ±HH:MM`.

2026-08-01 00:00:00 +05:30
- Established `change.log` as the permanent append-only project history file.
- Rule: every meaningful code, config, architecture, or documentation change must add a new summary entry here.
- Ordering rule: newest entries stay at the top.
- Retention rule: existing entries are not deleted or rewritten unless explicitly requested by the user.
